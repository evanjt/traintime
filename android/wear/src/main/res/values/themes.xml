<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the activity just needs a Wear base theme. -->
    <style name="Theme.TrainTimeWear" parent="@android:style/Theme.DeviceDefault" />
</resources>
